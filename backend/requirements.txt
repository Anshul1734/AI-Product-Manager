# Runtime dependencies.
#
# Retrieval runs on a pure-Python BM25 implementation with optional dense
# vectors fetched over HTTP, so there is no numpy, torch, or vector-database
# dependency here.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1

# Agent graph orchestration: supersteps, fan-out/fan-in joins, conditional
# edges, and thread-scoped checkpointing. Pulls langchain-core and
# langgraph-checkpoint.
langgraph==1.2.11
